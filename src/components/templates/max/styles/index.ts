// Max template styles
// Import the CSS file directly in components that need it
// CSS files cannot be exported from TypeScript modules

// If you need to import the CSS file, use:
// import './max.css';

export {}; // Empty export to make this a valid module